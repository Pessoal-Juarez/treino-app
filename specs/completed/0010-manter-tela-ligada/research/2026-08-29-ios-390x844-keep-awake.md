# Validação visual iOS — SPEC-0010

- Portal real: `SPEC0009`, `file:///C:/Users/Samsung/Documents/treino_v3/index.html`.
- Emulação: user agent `ios`; viewport confirmado em `390x844`.
- Fluxo real: em Ajustes, clique programático no controle real `#keepAwakeToggle` disparou o mesmo `change` do usuário. A leitura do portal retornou `[true,true,44,44,375,390]`: checkbox marcado, `state.keepAwakeEnabled` persistido no estado canônico, alvo 44×44 px e `scrollWidth` 375 dentro de 390 px.
- Após navegar novamente para o mesmo `file:///` e abrir Ajustes, o portal retornou `[true,44,44,375,390]`: preferência restaurada marcada, alvo 44×44 px e sem overflow horizontal.
- Captura: `2026-08-29-ios-390x844-keep-awake.png`, produzida localmente pelo Edge headless contra um harness temporário que apenas semeou a preferência, abriu Ajustes e centralizou o controle. Cabeçalho PNG confirmado em `390x844`.
- Limitação honesta: `maestri portal screenshot` não capturou porque a janela do portal estava minimizada. O snapshot/evaluate do portal continuou disponível; a imagem local complementa essa prova sem rede ou runtime remoto.
