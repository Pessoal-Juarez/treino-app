# Validação visual local — SPEC-0009

Data: 2026-08-29. Portal local `file:///C:/Users/Samsung/Documents/treino_v3/index.html`,
user agent iOS, viewport confirmado em 390×844.

O modal aberto por `openWorkoutImport()` expôs um seletor único de arquivo, a
área de texto e as ações **Ver prévia**, **Confirmar** e **Cancelar**. O snapshot
acessível confirmou os botões em 302×44 px. A inspeção DOM retornou `font-size`
de 16 px para seletor e textarea, e alturas de 44 px para as três ações.

No mesmo portal aberto por `file:`, `getLocalPdfJs()` retornou o módulo local
com `version: "5.4.54"`. Assim, a variante standalone resolve o parser por
caminho relativo do projeto, sem depender de `node_modules` ou de URL remota.

O screenshot do portal não pôde ser materializado porque a janela do canvas
estava minimizada; a evidência é o snapshot/DOM do portal ativo. Nenhuma rede,
upload ou dado real foi usado.
